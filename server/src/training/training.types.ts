import { Prisma } from '@prisma/client'
import { AttendanceRow } from './attendance.service'
export type TrainingCaller = { userId: bigint; roles: string[]; staffId?: bigint; memberId?: bigint }
export interface SessionRow { sessionId: bigint; memberId: bigint; member: { userId: bigint; user: { fullName: string } }; trainerStaffId: bigint; trainer: { userId: bigint; user: { fullName: string } }; roomId: bigint; room: { name: string }; assignmentId: bigint | null; assignment: { assignmentId: bigint; planId: bigint; plan?: { planId: bigint; name: string; description: string | null; status: string } | null } | null; planDayId: bigint | null; planDay: { planDayId: bigint; planId: bigint; dayNumber: number; weekNumber: number; dayOfWeek: number; name: string; notes?: string | null; exercises?: Array<{ planExerciseId: bigint; planDayId: bigint; exerciseId: bigint; orderIndex: number; targetSets: number; targetReps: number | null; targetDurationSec: number | null; targetWeightKg: Prisma.Decimal | number | string | null; restSeconds: number | null; notes: string | null; exercise?: { exerciseId: bigint; name: string; bodyPart: { name: string } | null; targetMuscle: { name: string } | null; equipment: { name: string } | null; description: string | null; gifUrl: string | null; imageUrl: string | null; createdByStaffId: bigint | null; createdAt: Date; deletedAt: Date | null } | null }> } | null; startTime: Date; endTime: Date | null; status: string; attendanceLogs?: AttendanceRow[] }
