import { MemberProgressService } from './member-progress.service'

describe('MemberProgressService', () => {
  it('is available as the progress boundary', () => {
    expect(MemberProgressService).toBeDefined()
  })
})
