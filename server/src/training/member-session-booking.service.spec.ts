import { MemberSessionBookingService } from './member-session-booking.service'

describe('MemberSessionBookingService', () => {
  it('is available as the member booking boundary', () => {
    expect(MemberSessionBookingService).toBeDefined()
  })
})
