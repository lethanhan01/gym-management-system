import { TrainingSessionService } from './training-session.service'

describe('TrainingSessionService', () => {
  it('is available as the session lifecycle boundary', () => {
    expect(TrainingSessionService).toBeDefined()
  })
})
