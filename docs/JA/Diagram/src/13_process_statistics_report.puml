@startuml
start

:ジムオーナーがレポート種類を選ぶ;
note right
例:
- 売上
- 新規登録
- 更新率
end note

:期間を選ぶ;

:システムがデータを取得;

:システムがデータを集計;
note right
データ元:
- 決済記録
- トレーニング履歴
end note

if (データがあるか?) then (Yes)
  :グラフと表で出力;
  :ジムオーナーが確認;
else (No)
  :データがないと表示;
endif

stop
@enduml